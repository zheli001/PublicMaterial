Working as realtor to create application pacakge from builder's website and provided package.

You could have the package that builder provided in a directory which it could include material in pdf, vidoe, images, forms etc
There are more information from their website.

You should gather all these material and create application package for potential client, but the material might need to be customized with your information. You should highlight the section which need to be modified. For example: the agent contact information

Agent information might be hard coded in application package for client to fill in.

The Input would be material directory and builder website for the project.
The Output would be final application package directory that agent can send to client for next step.
